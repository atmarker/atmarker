name: Update badges

on:
  manual
  schedule:
    # Runs at 2am UTC
    - cron: "0 2 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: atmarker/atmarker@main
        with:       
          CREDLY_USER: atmarker
